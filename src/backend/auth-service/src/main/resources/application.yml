spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth-service?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ad_on_wheels_user
    password: test

jwt:
  secret: ${JWT_SECRET_KEY}

jpa:
  hibernate:
    ddl-auto: update
  show-sql: true
  database-platform: org.hibernate.dialect.MySQL8Dialect

application:
  name: auth-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8085