spring:
  datasource:
    url: jdbc:mysql://localhost:3306/company-service?useSSL=false&serverTimezone=UTC
    username: ad_on_wheels_user
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  application:
    name: company-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8083